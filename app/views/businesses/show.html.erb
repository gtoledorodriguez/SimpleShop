<div class="row">
  <div class="offset-md-3 col-md-6">
    <%= render @business %>

    <div class="mt-3 d-flex gap-2">
      <%= link_to "Edit this business", edit_business_path(@business), class: "btn btn-outline-primary" %>
      <%= link_to "Back to businesses", businesses_path, class: "btn btn-outline-secondary" %>
      <%= button_to "Destroy this business", @business, method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-danger" %>
    </div>
  </div>
</div>
