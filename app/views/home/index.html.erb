<h1>Welcome to SimpleShop</h1>
<% if user_signed_in? %>
  <p>Hello <%= current_user.name %>! Go to your <%= link_to "Inventory", items_path %>.</p>
<% else %>
  <p>
    <%= link_to "Sign Up", new_user_registration_path %> or 
    <%= link_to "Log In", new_user_session_path %>
  </p>
<% end %>
